﻿@using Air.ViewModel;
@using AirDataModels;
@model ReservationItem
<style>
    .PLLO{
        margin-left:20px;

    }
    .textemission-left{
        font-weight:bold;
        font-size:14px;
margin-top: 8px;
    }
</style>

<div class="col-10 col-sm-10 col-md-3 col-lg-3 col-xl-3 col-xxl-3 PL0 text-left">
    @* <div class="tblIconThumb"><img src="~/assets/images/listing/Airline/@(Model.MarketingAirline.AirlineCode).png" style="width:60px; height:40px" alt=""></div> *@
    <div class="tblIconThumb">
        <img src="~/assets/images/listing/Airline/@(Model.MarketingAirline.AirlineCode).png" style="width:36px;" alt="">
    </div>

    <span class="colorGrey font12">@(Model.MarketingAirline.AirlineName.Count() > 16 ? Model.MarketingAirline.AirlineName.Substring(0, 14) + ".." : Model.MarketingAirline.AirlineName)</span>
</div>
<div class="col-12 col-sm-12 col-md-4 col-lg-4 col-xl-4 col-xxl-4 textAlignCenter">
    <div class="row city-flight-info-wrap">
        <div class="col-4 col-sm-4 col-md-6 col-lg-6 col-xl-6 col-xxl-6 colorGrey city-name">
            <small class="allignTop">@Model.DepartureAirport.CityName</small>
            <div class="timeBold">@Model.DepartureDateTime.ToString("HH:mm")</div>
        </div>
        <div class="col-4 col-sm-4 col-md-6 col-lg-6 col-xl-6 col-xxl-6 colorGrey city-name">
            <small class="allignTop">@Model.ArrivalAirport.CityName</small>
            <div class="timeBold">@Model.ArrivalDateTime.ToString("HH:mm")</div>
        </div>
    </div>
    <div class="flightBgright"></div>
</div>
<div class="col-12 col-sm-12 col-md-4 col-lg-4 col-xl-4 col-xxl-4 PL0 PR0 textAlignCenter">
    <div class="duration">
        @{
            string val = string.Empty;
            if (Model.StopQuantity == 0)
            {
                val = "Non Stop";
            }
            else
            {
                val = Model.StopQuantity + " Stop(s)";
            }
        }
        @Model.JourneyDuration <span class="l-divider"> | </span> <span class="colorGrey">@Model.StopQuantityInfo.LocationCode</span> @val*
    </div>  
    <div class="textemission-left">
        @((Model.EmissionsInKg != null ? Model.EmissionsInKg.ToString() : "0.0")) Kg CO<sub>2</sub>
    </div>

</div>


@* <div class="col-6 textemission-left">@Model.EmissionsInKg Kg CO<sub>2</sub></sub></div> *@
